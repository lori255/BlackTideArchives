# v0.5 仍未完成项

## 还缺什么

- 角色透明立绘：当前未通过人工级抠图，不能保证透明边缘可直接商用。
- 大批量异常物：当前新增 2 件生产候选，完整游戏仍需更多。
- 完整路线/状态/材料 SVG 图标全集：当前补了核心 9 个，后续还需按系统配置补齐。
- 动画序列：还没有粒子序列、角色动态、按钮动效帧。
- 移动端切图：尚未按 1x/2x/3x 或不同 DPR 输出完整版本。
- 版权与相似性检查：所有 AI 图商用前仍需复核。

## 已经比 v0.4 改进的地方

- 生产候选素材采用单独生成，不再依赖大图集标题区裁切。
- 新增素材无 UI 标题、编号、中文说明。
- 新增前端接入示例和 production manifest 标记。
